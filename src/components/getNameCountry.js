function getNameCountry(string) {
    let arr = string.split(' ');
    console.log(arr.length);
    if (arr.length<=2) {
        return string;
    }
    let  firstWordsString = arr.slice(0, 2).join(  );
      return firstWordsString
}

export default getNameCountry;