import refs from '../utils/refs';


function getDefaultSetting(){
    refs.menu.innerHTML="";
    refs.card.innerHTML="";
    refs.wrapper.style.background = ``;
};

export default getDefaultSetting;